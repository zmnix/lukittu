version: '3.8'
services:
  prisma:
    image: node:latest
    env_file: .env
    working_dir: /app
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - .:/app
    networks:
      - lukittu-network
    command: sh -c "npx prisma generate && npx prisma migrate deploy"

networks:
  lukittu-network:
    external: true
